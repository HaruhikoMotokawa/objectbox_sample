---
alwaysApply: true
---
# Flutter 基本コーディングルール 📝

## 1. プロジェクト参照
このリポジトリの詳細 @./.github/copilot-instructions.md

## 2. 文字列をハードコードする際のルール
- 一行が`80`文字を超えた場合は折り返す
- 改行処理を行った後、`Missing whitespace between adjacent strings.Try adding whitespace between the strings.`エラーを修正
  - その際`+`での連結は使用しない。改行した状態を保つ

```dart
// ❌ 80文字を超える長い文字列
Text('これは非常に長いテキストで、80文字を超えてしまうような場合の例です。このような場合は分割する必要があります。')

// ✅ 80文字以内で分割（隣接する文字列リテラルは自動連結される）
Text(
  'これは非常に長いテキストで、80文字を超えてしまうような場合の例です。'
  'このような場合は分割する必要があります。'
)
```

## 3. クラス作成ルール
- `Util`など、`static`なメソッドだけで構成されるクラスは`abstract final class`とする

## 4. 技術選択ルール
- **非同期操作:** 非同期操作の処理には`async/await`を使用

## 5. nullハンドリングルール
- **`!`演算子の使用を極力避ける:** `value!.params`のような書き方は避け、必ずnullチェックを行う
- **安全なnullチェック方法を使用:**
  - 変数に代入してからnullチェック
  - if-case構文を使用したパターンマッチング

```dart
class User {
  final int? phoneNumber;
}

// ❌ 避けるべき書き方
final result = user.phoneNumber!.toString();

// ✅ 推奨する書き方1: 変数に代入してnullチェック
final num = user.phoneNumber;
if (num != null) {
  final result = num.toString();
}

// ✅ 推奨する書き方2: if-case構文
if (user.phoneNumber case final num?) {
  final result = num.toString();
}

// ✅ 推奨する書き方3: null-aware演算子
final result = user.phoneNumber?.toString() ?? 'なし';
```

## 6. 条件式の可読性ルール
- **複雑な条件式は変数に抽出する:** 読み取りにくい条件や複数の条件を組み合わせる場合は、意味のある変数名で抽出する
- **条件の意図を明確にする:** 変数名で条件の目的や意味を表現する

```dart
// ❌ 避けるべき書き方
if (flag1 == true && flag2 != null && flag3 == true) {
  // 処理
}

if (user.age >= 18 && user.hasLicense && !user.isSuspended) {
  // 処理
}

// ✅ 推奨する書き方
final shouldUpdate = flag1 == true && flag2 != null && flag3 == true;
if (shouldUpdate) {
  // 処理
}

final canDrive = user.age >= 18 && user.hasLicense && !user.isSuspended;
if (canDrive) {
  // 処理
}
```

## 7. メソッド引数の可読性ルール
- **メソッドの引数には原則変数を使用:** 複雑な式や連鎖的なアクセスを直接引数に渡さず、変数に抽出してから渡す
- **引数の意図を明確にする:** 変数名で引数の意味や用途を表現する

```dart
// ❌ 避けるべき書き方
update(userId: data.user.id);
save(name: user.profile.personalInfo.fullName);
process(isValid: form.isComplete && user.hasPermission);

// ✅ 推奨する書き方
final userId = data.user.id;
update(userId: userId);

final fullName = user.profile.personalInfo.fullName;
save(name: fullName);

final isValidForProcessing = form.isComplete && user.hasPermission;
process(isValid: isValidForProcessing);
```

## 8. 避けるべきコーディング
- **管理されていない状態:** `StreamSubscription`や`AnimationController`などのリソースの破棄を忘れることによるメモリリーク
- **エラーの無視:** 例外を適切に処理しないと予期しないクラッシュにつながる。`try-catch`ブロックとログを使用

## 9. エラーハンドリングの書き方
- 例外処理には`try-catch`ブロックを使用
- プログラムロジック上あり得ない状況では`StateError`を使用
  ```dart
  // 例：前画面で表示したデータのIDで検索して見つからない場合
  final work = await repository.findWorkById(workId);
  if (work == null) {
    throw StateError('表示中の作品が見つかりません: $workId');
  }
  ```
- 開発段階で気づくべきエラーには`assert`を使用
  ```dart
  // 例：必要な引数が渡されていない場合
  Widget buildButton({required String text, VoidCallback? onPressed}) {
    assert(text.isNotEmpty, 'text cannot be empty');
    assert(onPressed != null, 'onPressed callback is required');
    // ...
  }
  ```
- デバッグのためにエラーをファイルやリモートサービスにログ出力
- ユーザーフレンドリーなエラーメッセージを表示
