---
alwaysApply: true
---
# Flutter プロジェクト構造ルール 📁

## 📁 ファイル構成・命名統一ルール

### ディレクトリ構成パターン
```
lib/
├── data/repositories/my_feature/
│   ├── exceptions/
│   │   └── my_feature_exception.dart
│   ├── providers/
│   │   ├── my_feature_provider.dart
│   │   └── my_feature_provider.g.dart
│   ├── my_feature_repository.dart
│   ├── my_feature_repository.part_extension.dart
│   └── my_feature_repository.part_private.dart
├── applications/services/my_feature/
│   ├── my_feature_service.dart
│   └── providers/
└── presentations/screens/my_feature/
```

### ファイル責務の統一
- **main file**: 公開インターフェース
- **part_extension**: 拡張メソッド
- **part_private**: プライベートメソッド
- **providers/**: Riverpodプロバイダー定義
- **exceptions/**: ドメイン固有例外

### 命名規則
- **ファイル名**: `snake_case`使用（例: `user_profile_screen.dart`）
- **クラス名**: ファイル名を反映（例: `user_profile_screen.dart`には`UserProfileScreen`を含む）
- **Key定義**: `snake_case`使用（既存は変更しない）
- **例外**: 関連する複数のenum、typedef、小さなヘルパー関数を一つのファイルにまとめることは、明確性が向上する場合は許可

## ⚙️ ビルドプロセス統一ルール

### 自動コード生成対象
- **Riverpodプロバイダー** (`@riverpod`, `@Riverpod()`)
- **Isarエンティティ** (`@collection`)
- **テストモック** (`@GenerateNiceMocks`)

### ビルドコマンド実行タイミング
以下の変更時は必ず`derry build`を実行：
```bash
derry build
```

**実行が必要な場合:**
- riverpodのproviderの新規作成・更新・削除
- isarのentityの新規作成・更新・削除
- mokitoを使ったテストモックの新規作成・更新・削除
- freezedを使ったモデルの新規作成・更新・削除
- go_router_builderを使ったルートの新規作成・更新・削除

### 実行フロー
1. ファイル保存
2. `derry build`実行
3. 生成ファイルの確認
4. テスト実行

## 🚀 Flutter バージョン管理ルール

### FVM（Flutter Version Management）使用
- **バージョン管理**: プロジェクトでは`FVM`を使用してFlutterバージョンを管理
- **Flutterコマンド実行**: 単体でflutterコマンドを実行する場合は必ず`fvm flutter`を使用

```bash
# ❌ 避けるべき書き方
flutter --version
flutter pub get
flutter build apk

# ✅ 推奨する書き方
fvm flutter --version
fvm flutter pub get
fvm flutter build apk
```

### FVM使用ルール
- **プロジェクト固有バージョン**: `.fvm/fvm_config.json`で指定されたバージョンを使用
- **コマンド実行**: AI生成コードやコマンド提案時も`fvm flutter`を前置
- **チーム開発**: 全開発者が同一のFlutterバージョンを使用することを保証

## 🔄 作業が完了した後に行うフロー
- **Lint問題を全て修正する**（エラー、警告、提案を含む）
  - `dart analyze`でエラー・警告・提案を確認
  - コード品質向上のため、全ての指摘事項を修正
- **スペルチェックを実行し、修正する**
  - コメント、変数名、クラス名等の英語スペルミスを確認
  - 適切な英語表記に修正してコードの可読性を向上
- 関連テストの実行、修正を行う
- 必要に応じて`derry build`を実行
